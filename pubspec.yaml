name: screenshots_example
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/fonts/
    - assets/images/
  fonts:
   - family: 'Roboto'
     fonts:
     - asset: assets/fonts/Roboto-Regular.ttf
  uses-material-design: true
